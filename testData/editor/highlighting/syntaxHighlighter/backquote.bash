#!/usr/bin/env bash

#The backquote characters are lexer highlights and not handled by the annotator
`<info textAttributesKey="BASH.INTERNAL_COMMAND">echo</info> <info textAttributesKey="BASH.BACKQUOTE">hello</info> $var`